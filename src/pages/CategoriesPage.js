import React from 'react';
import Button from '../components/Button';

const CategoriesPage = () => (
  <div>
    <h2>Book&apos;s Categories</h2>
    <p>Is unavailable right now.</p>
    <Button
      buttonType="button"
      text="Check Status"
      buttonClass="checkStatus"
    />
  </div>
);

export default CategoriesPage;
